<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or "Presentation" }}</title>
    <link rel="icon" type="image/svg+xml" href="/assets/{{ favicon or 'favicon.svg' }}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@400;600;700;800&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/theme.css">
    {% block head %}{% endblock %}
    {# Preload background images if this is a presentation #}
    {% if rawContent %}
        {% set bgImages = rawContent | extractBgImages %}
        {% for bgImage in bgImages %}
    <link rel="preload" as="image" href="{{ bgImage }}">
        {% endfor %}
    {% endif %}
</head>
<body>
    {% block content %}
    {{ content | safe }}
    {% endblock %}
</body>
</html>
